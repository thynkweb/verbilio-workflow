import { FaXTwitter } from "react-icons/fa6";

function TwitterLogo(props: { size: number; color: string }) {
  return <FaXTwitter size={props.size} color={props.color} />;
}

export default TwitterLogo;
