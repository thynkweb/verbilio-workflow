import { FaInstagram } from "react-icons/fa6";

function InstagramIcon(props: { size: number; color: string }) {
  return <FaInstagram size={props.size} color={props.color} />;
}

export default InstagramIcon;
